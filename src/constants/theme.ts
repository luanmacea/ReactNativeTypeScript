import { createTheme } from '@rneui/themed';

export const defaultTheme = createTheme({
  lightColors: {
    primary: '#ff5712',
    secondary: '#000000',
    background: '#F5F5F5'
  },
  mode: 'light'
});
